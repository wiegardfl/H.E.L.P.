<Window x:Class="HELP.Forms.PatientList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HELP.Forms"
        mc:Ignorable="d"
        Title="Patienten" Height="650" Width="600" ResizeMode="NoResize" Background="#FFDCD9D9">
    <Window.Resources>
        <Style TargetType="{x:Type Button}">
            <Setter Property="BorderThickness" Value="2" />
        </Style>
    </Window.Resources>
    <Grid Margin="30 20 30 30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*" />
            <RowDefinition />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <DockPanel>
            <TextBox x:Name="Search" Width="420" VerticalAlignment="Center" Padding="4" TextChanged="Search_TextChanged" />
            <Button x:Name="SelectBtn" Content="Auswählen" VerticalAlignment="Center" Padding="4" Margin="10 0 0 0" Click="SelectBtn_Click" />
        </DockPanel>

            <ListView x:Name="PatientsListView" Grid.Row="1" SelectionMode="Single">
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding FullName}" Width="200" />
                        <GridViewColumn Header="KV-Nr." Width="178" DisplayMemberBinding="{Binding KVNR}" />
                        <GridViewColumn Header="Geburtsdatum" Width="150" DisplayMemberBinding="{Binding Geburtsdatum}" />
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>

        <Button x:Name="AddBtn" Grid.Row="2" Content="Neuen Patienten anlegen" Margin="0 10" Click="AddBtn_Click" />
    </Grid>
</Window>
